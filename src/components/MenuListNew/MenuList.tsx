import {MenuByCategory} from "../../types";

export const MenuList = ({ menusByCategory }: { menusByCategory: MenuByCategory }) => {
  return <div>This is the new Menu list with more information</div>;
}